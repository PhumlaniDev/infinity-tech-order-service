<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Console Appender -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Loki Appender -->
  <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
    <http>
      <url>http://loki:3100/loki/api/v1/push</url>
      <connectTimeoutMs>1000</connectTimeoutMs>
      <readTimeoutMs>1000</readTimeoutMs>
    </http>

    <batch>
      <maxSize>100</maxSize>
      <maxTimeMs>5000</maxTimeMs>
    </batch>

    <label>
      <label>app=order-service</label>
      <label>env=dev</label>
      <label>level=%level</label>
      <label>thread=%thread</label>
    </label>

    <message>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] >>> %msg%n</pattern>
    </message>
  </appender>

  <!-- Enable debug logs for Loki (optional for troubleshooting) -->
  <logger name="com.github.loki4j" level="DEBUG"/>

  <!-- Root Logger -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="LOKI"/>
  </root>

</configuration>